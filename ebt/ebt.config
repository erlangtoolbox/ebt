{version, {shell, "../version.sh"}}.

{targets, [
    {perform, [escript, otpapp]}
]}.

{compile, [
    {flags, [warnings_as_errors, debug_info]}
]}.

{libraries, ["../lib"]}.

{escript, [
    {ebt, "-noshell -noinput +d", ["priv/*"]}
]}.
