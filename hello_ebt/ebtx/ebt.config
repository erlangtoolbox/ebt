{libraries, ["./lib"]}.

{profiles, [
    {default, [
        {prepare, [depends]}
    ]}
]}.

{depends, [
    {dir, "./lib"},
    {repositories, [
        {"http://erlang-build-tool.googlecode.com/files", [
            {ebt, "1.4.3"}
        ]}
    ]}
]}.
